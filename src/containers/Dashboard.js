import React from 'react';

const Dashboard = () => (
  <div
    style={
      {
        display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 50,
      }
    }
  >
    <div style={{ color: '#2da68a', fontSize: '17px', fontWeight: 'bolder' }}>STATISTICAL DASHBOARD</div>
  </div>
);

export default Dashboard;
